# RL training dependencies (WSL venv).
# Superset of root requirements.txt — adds PyTorch + Ray RLlib + Gymnasium.

# Inherit live-engine dependencies
-r ../requirements.txt

# Reinforcement learning
torch>=2.0.0
ray[rllib]>=2.9.0
gymnasium>=0.29.0
tqdm>=4.65.0

# Optional: training observability
tensorboard>=2.15.0
